import { describe, it, expect } from "vitest";
import { validateField } from "../src/validatefield";

describe("Test the valdate field function with accurate data", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
});

describe("Test the valdate field function with field data", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
});

describe("Test the valdate field function with inaccurate data", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
});
